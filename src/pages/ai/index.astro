---
import fs from 'node:fs';
import path from 'node:path';
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const docs = await getCollection('ai', ({ data }) => data.published);

interface AiEntry {
  slug: string;
  title: string;
  description?: string;
  date: string; // YYYY-MM-DD when known, else ''
  dateLabel: string;
  kind: string; // 'writing' | 'html' | custom
  model?: string;
  href: string;
}

/** Extract <meta name="ai-model" content="..."> from a raw html string. */
const extractAiModel = (html: string): string | undefined => {
  const re = /<meta\s+[^>]*name=["']ai-model["'][^>]*>/i;
  const tag = html.match(re)?.[0];
  if (!tag) return undefined;
  const content = tag.match(/content=["']([^"']+)["']/i)?.[1];
  return content?.trim() || undefined;
};

const parseLocalDate = (dateString: string) => {
  const match = /^(\d{4})-(\d{2})-(\d{2})$/.exec(dateString);
  if (!match) return null;
  const [, year, month, day] = match;
  return new Date(Number(year), Number(month) - 1, Number(day));
};
const formatShort = (date: Date) => {
  const month = date.toLocaleDateString('en-US', { month: 'short' }).toLowerCase();
  const day = String(date.getDate()).padStart(2, '0');
  return `${month} ${day} ${date.getFullYear()}`;
};

const mdEntries: AiEntry[] = docs.map((doc) => {
  const parsed = parseLocalDate(doc.data.pubDate);
  return {
    slug: doc.id,
    title: doc.data.title,
    description: doc.data.description,
    date: doc.data.pubDate,
    dateLabel: parsed ? formatShort(parsed) : doc.data.pubDate,
    kind: doc.data.kind ?? 'writing',
    model: doc.data.model,
    href: `/ai/${doc.id}`,
  };
});

// Scan src/content/ai for raw .html files (served via /ai/[slug]).
const aiDir = path.resolve('./src/content/ai');
const htmlEntries: AiEntry[] = [];
if (fs.existsSync(aiDir)) {
  for (const file of fs.readdirSync(aiDir)) {
    if (!file.endsWith('.html')) continue;
    const slug = file.replace(/\.html$/, '');
    const filePath = path.join(aiDir, file);
    const stat = fs.statSync(filePath);
    const mtime = stat.mtime;
    const ymd = mtime.toISOString().slice(0, 10);
    const raw = fs.readFileSync(filePath, 'utf-8');
    htmlEntries.push({
      slug,
      title: slug.replace(/[-_]/g, ' '),
      date: ymd,
      dateLabel: formatShort(mtime),
      kind: 'html',
      model: extractAiModel(raw),
      href: `/ai/${slug}`,
    });
  }
}

const allEntries = [...mdEntries, ...htmlEntries].sort((a, b) =>
  b.date.localeCompare(a.date),
);
---
<Layout
  title="ai corner"
  description="A small corner for AI-generated writings and dropped html files. Not written by anuraag."
  robots="noindex,follow"
>
  <section class="ai-corner">
    <header class="corner-head">
      <div class="kicker">// ai corner</div>
      <h1 class="corner-title">ai corner</h1>
      <p class="corner-blurb">
        a place for ai-generated writings and <code>.html</code> files.
      </p>
      <p class="corner-note">
        my own posts live on the <a href="/">main index</a>.
      </p>
    </header>

    {allEntries.length === 0 ? (
      <p class="empty">nothing here yet — drop <code>.md</code> or <code>.html</code> files into <code>src/content/ai/</code>.</p>
    ) : (
      <ol class="entry-list">
        {allEntries.map((entry, idx) => {
          const num = allEntries.length - idx;
          const hex = `0x${num.toString(16).padStart(2, '0')}`;
          return (
            <li class="entry-row">
              <a class="entry-link" href={entry.href}>
                <span class="entry-num">{hex}</span>
                <span class="entry-body">
                  <span class="entry-title">{entry.title}</span>
                  {entry.description && (
                    <span class="entry-desc">{entry.description}</span>
                  )}
                </span>
                <span class="entry-meta">
                  <span class="entry-kind">{entry.kind}</span>
                  {entry.model && (
                    <>
                      <span class="entry-sep" aria-hidden="true">·</span>
                      <span class="entry-model">{entry.model.toLowerCase()}</span>
                    </>
                  )}
                  <span class="entry-sep" aria-hidden="true">·</span>
                  <span class="entry-date">{entry.dateLabel}</span>
                </span>
              </a>
            </li>
          );
        })}
      </ol>
    )}
  </section>
</Layout>

<style>
  .ai-corner {
    max-width: 820px;
    margin: 0 auto;
    padding: 1rem 1rem 4rem;
    font-family: var(--font-mono);
  }

  .corner-head {
    padding-bottom: 1.5rem;
    margin-bottom: 1.75rem;
    border-bottom: 1px solid var(--border-soft);
  }

  .kicker {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    color: var(--fg-subtle);
    text-transform: lowercase;
    margin-bottom: 0.6rem;
  }

  .corner-title {
    margin: 0 0 0.6rem;
    font-size: 2rem;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: var(--fg-strong);
  }

  .corner-blurb {
    margin: 0 0 0.4rem;
    font-size: 0.92rem;
    line-height: 1.7;
    color: var(--fg-muted);
    max-width: 56rem;
  }

  .corner-blurb code,
  .corner-note code,
  .empty code {
    font-size: 0.85em;
    background: var(--surface-elev);
    padding: 0.06rem 0.32rem;
    border-radius: 3px;
    color: var(--fg);
  }

  .corner-note {
    margin: 0;
    font-size: 0.82rem;
    color: var(--fg-subtle);
  }

  .corner-note a {
    color: var(--fg-muted);
    text-decoration: none;
    border-bottom: 1px dashed var(--border-strong);
  }

  .corner-note a:hover {
    color: var(--fg);
    border-bottom-color: var(--fg);
  }

  .empty {
    color: var(--fg-muted);
    font-size: 0.9rem;
    padding: 1rem 0;
  }

  .entry-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .entry-row {
    border-bottom: 1px dashed var(--border-soft);
  }

  .entry-row:first-child {
    border-top: 1px dashed var(--border-soft);
  }

  .entry-link {
    display: grid;
    grid-template-columns: 3.5rem 1fr auto;
    gap: 0.85rem;
    align-items: center;
    padding: 0.85rem 0.5rem 0.85rem 0;
    text-decoration: none;
    color: inherit;
    transition: background 0.12s;
  }

  .entry-link:hover {
    background: rgba(var(--shadow-rgb), 0.07);
  }

  .entry-num {
    font-size: 0.78rem;
    color: var(--fg-faint);
    font-weight: 600;
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.03em;
  }

  .entry-body {
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  .entry-title {
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--fg);
    line-height: 1.3;
  }

  .entry-link:hover .entry-title {
    color: var(--fg-strong);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .entry-desc {
    margin-top: 0.2rem;
    font-size: 0.8rem;
    color: var(--fg-muted);
    line-height: 1.5;
  }

  .entry-meta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.72rem;
    color: var(--fg-faint);
    white-space: nowrap;
  }

  .entry-kind {
    color: var(--fg-subtle);
    text-transform: lowercase;
    font-weight: 500;
    letter-spacing: 0.04em;
  }

  .entry-model {
    color: var(--fg-faint);
    font-style: italic;
    letter-spacing: 0.02em;
  }

  .entry-sep {
    color: var(--border-strong);
  }

  .entry-date {
    color: var(--fg-subtle);
    font-variant-numeric: tabular-nums;
  }

  @media (max-width: 640px) {
    .entry-link {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }

    .entry-num {
      display: none;
    }

    .entry-meta {
      font-size: 0.7rem;
    }
  }
</style>
